#include <iostream>
#include <fstream>
#include <vector>

using namespace std;


int main(){
    int Levels, Y_axis, X_axis;

    while (cin >> Levels >> Y_axis >> X_axis){
        if(Levels == 0 && Y_axis == 0 && X_axis == 0){
            break;
        }

        for(int l = 0; l < Levels; l++){
            for(int y = 0; y < Y_axis; y++){
                for(int x = 0; x < X_axis; x++){
                    
                }
            }
        }

    }
    

    return 0;
}